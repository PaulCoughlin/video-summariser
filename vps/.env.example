# Copy to .env and fill in. Never commit the real .env.

# Required: get from https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Optional: shown in OpenRouter analytics so you can see traffic by app.
OPENROUTER_REFERER=https://github.com/PaulCoughlin/video-summariser
OPENROUTER_APP_TITLE=Video Summariser

# Optional: per-IP daily limit (default 10).
RATE_LIMIT_PER_DAY=10

# Optional: residential proxy for YouTube transcripts. Datacenter IPs (any VPS)
# are usually blocked by YouTube; set both to route transcript requests through
# Webshare's residential pool. Get credentials from https://www.webshare.io/
# (Residential plan, ~$3/month).
WEBSHARE_PROXY_USERNAME=
WEBSHARE_PROXY_PASSWORD=
